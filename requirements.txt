# Player Tracking & Heatmap Generation
# Core dependencies
ultralytics>=8.1.0
supervision>=0.18.0
opencv-python-headless>=4.7.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
PyYAML>=6.0
tqdm>=4.65.0

# Note: ultralytics installs a compatible PyTorch. For GPU builds, see README for instructions.
